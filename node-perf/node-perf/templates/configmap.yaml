apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "node-perf.fullname" . }}-scripts
  labels:
    {{- include "node-perf.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "files/*").AsConfig | indent 2 }}
