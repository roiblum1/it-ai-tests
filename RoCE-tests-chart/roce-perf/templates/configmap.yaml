apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "roce-perf.fullname" . }}-scripts
  labels:
    {{- include "roce-perf.labels" . | nindent 4 }}
data:
{{ (.Files.Glob "files/*").AsConfig | indent 2 }}
